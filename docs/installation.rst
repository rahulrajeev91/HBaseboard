============
Installation
============

At the command line::

    $ easy_install HBaseBoard

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv HBaseBoard
    $ pip install HBaseBoard
